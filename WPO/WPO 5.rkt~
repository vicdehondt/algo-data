#lang r7rs
(import (scheme base)
        (scheme write)
        (scheme cxr))

; Oef 1
(define exercise-a (list 'exercise-a
                         3
                         eq?
                         (vector 5 6 3 0 0 0 0 0 0 0 0 0)))

(define-record-type (exercise-a)
  (make ))